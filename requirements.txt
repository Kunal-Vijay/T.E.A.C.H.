# Backend dependencies.
#
# Requires Python 3.12+ because aws-sdk-bedrock-runtime (the experimental SDK for
# Bedrock bidirectional streaming, used by the Nova Sonic voice feature) sets a
# 3.12 floor. Everything else works on 3.11.
#
#   /opt/homebrew/bin/python3.12 -m venv venv312
#   ./venv312/bin/python -m pip install -r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.2
sqlalchemy==2.0.36
pydantic-settings==2.7.0
google-genai==2.17.0
gTTS==2.5.4
mangum==0.19.0
alembic==1.14.0
python-multipart==0.0.20
boto3==1.43.66
httpx==0.28.1
python-dotenv==1.0.1

# Amazon Nova Sonic speech-to-speech (bidirectional streaming over Bedrock).
aws-sdk-bedrock-runtime==0.8.0
smithy-aws-core==0.8.0
